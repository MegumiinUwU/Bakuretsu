# Platform logo overrides

Bakuretsu draws simple built-in logos for each platform. To use an official
logo instead, drop a square PNG here named after the platform key:

- `backloggd.png`
- `letterboxd.png`
- `myanimelist.png`

Any file present here automatically overrides the drawn logo.
