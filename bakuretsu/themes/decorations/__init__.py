"""Theme decoration drawers.

Each module exposes ``draw(card, style)`` which composites decorative
artwork onto the card *before* the content is rendered. Register new
decorations in ``themes/registry.py``.
"""
